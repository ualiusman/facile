@using HandoverTracker.Models;
@using Microsoft.AspNet.Identity;
@model Tuple<UserViewModel, ManageUserViewModel>
@{
    ViewBag.Title = "Manage Account";
    
}



    <div style="color: #008CBA;">@ViewBag.StatusMessage</div>

<div class="row">
    <div class="large-12 columns">
        @Html.Partial("_ManageAccountInfo", Model.Item1)
    </div>
</div>

    <div class="row">
        <div class="large-12 columns">
            @if (ViewBag.HasLocalPassword)
            {
                @Html.Partial("_ChangePasswordPartial",Model.Item2)
            }
            else
            {
                @Html.Partial("_SetPasswordPartial")
            }

            @*<section id="externalLogins">
                    @Html.Action("RemoveAccountList")
                    @Html.Partial("_ExternalLoginsListPartial", new { Action = "LinkLogin", ReturnUrl = ViewBag.ReturnUrl })
                </section>*@
        </div>
    </div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
